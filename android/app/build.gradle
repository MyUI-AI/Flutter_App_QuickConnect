plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.quick_connect_application"
    compileSdk = 34  // Updated to SDK version 34

    //ndkVersion = "23.1.7779620"  // Set your NDK version if necessary, otherwise you can remove this

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = "1.8"
    }

    defaultConfig {
        applicationId = "com.example.quick_connect_application"
        minSdk = 21  // You can adjust this to suit your app requirements
        targetSdk = 34  // Updated to SDK version 34
        versionCode = 1  // Update as necessary for your versioning
        versionName = "1.0.0"
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug  // Adjust this for release if needed
        }
    }
}

flutter {
    source = "../.."
}
